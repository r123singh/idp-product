For **GLOBAL Amazon Nova Lite** (Bedrock Global), using the currently listed rates:

- **Input:** `$0.06 per 1M tokens`
- **Output:** `$0.24 per 1M tokens`

With your usage:

- Input tokens = `2932`
- Output tokens = `3308`

Cost:

- Input cost = `(2932 / 1,000,000) × 0.06 = $0.00017592`
- Output cost = `(3308 / 1,000,000) × 0.24 = $0.00079392`

**Total run cost = `$0.00096984`** (about **$0.00097**, i.e. ~0.097 cents)

You can reuse this for Nova Lite Global:

```
Total = (inputTokens * 0.06 + outputTokens * 0.24) / 1000000
```

For **Claude 4.5 Sonnet** (Bedrock Global), using the currently listed rates:

- **Input:** `$0.06 per 1M tokens`
- **Output:** `$0.24 per 1M tokens`
